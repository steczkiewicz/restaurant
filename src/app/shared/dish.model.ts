export interface Dish {
    dish_name: string,
    image: string,
    category: string,
    cuisine: string,
    description: string,
    ingredients: string,
    quantity: number,
    price: number,
    id: string
}